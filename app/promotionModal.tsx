import React from 'react'
import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const promotionModal = () => {
  return (
    <SafeAreaView>
      <Text>promotionModal</Text>
    </SafeAreaView>
  )
}

export default promotionModal